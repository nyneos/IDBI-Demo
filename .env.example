# Local dev — Vite proxies /api to this URL
VITE_API_BASE_URL=http://localhost:8081

# Production (Vercel) — set the same variable in Vercel → Settings → Environment Variables
# VITE_API_BASE_URL=https://your-api.onrender.com
